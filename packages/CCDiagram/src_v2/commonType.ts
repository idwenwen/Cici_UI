export type Combinable = any;

export type Duration = number; // length of time
export type Progress = number; // rate of progress
export type Dictionary = {
  // Dictionary mapping table
  [key: string]: string;
};
